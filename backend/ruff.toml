[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines — not worth fighting in a startup codebase

[lint.per-file-ignores]
"tests/*" = ["F401"]  # test helpers may import for side effects
